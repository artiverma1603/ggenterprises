import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { MatDialogModule } from '@angular/material/dialog';

const data = [MatDialogModule];
@NgModule({
  declarations: [],
  imports: [data],
  exports: [data],
})
export class MaterialModule {}
